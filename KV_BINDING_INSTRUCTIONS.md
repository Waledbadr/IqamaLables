# خطوات ربط KV Namespace في Cloudflare Dashboard

## ✅ تم إنجازه

1. ✅ إنشاء KV Namespace للإنتاج
   - ID: `d1ce93d311c34504939483fbff10c523`

2. ✅ إنشاء KV Namespace للـ Preview
   - Preview ID: `b68cd2fe859f47e5bf4a505cb870ff1a`

3. ✅ تحديث `wrangler.toml` بالـ IDs
4. ✅ رفع التعديلات إلى GitHub

---

## 🔧 الخطوة الأخيرة (يدوية)

### افتح لوحة تحكم Cloudflare Pages:

1. اذهب إلى: https://dash.cloudflare.com
2. اختر حسابك
3. اذهب إلى **Pages**
4. اختر مشروع **IqamaLables**

### اربط KV Namespace:

1. اذهب إلى **Settings** (الإعدادات)
2. اختر **Functions** من القائمة الجانبية
3. انزل إلى قسم **KV namespace bindings**
4. اضغط **Add binding**
5. املأ الحقول:
   - **Variable name**: `PRESETS`
   - **KV namespace**: اختر `PRESETS` من القائمة المنسدلة

6. اضغط **Save**

---

## 🚀 اختبار النظام

بعد ربط KV Namespace، سيتم إعادة نشر التطبيق تلقائياً.

### اختبار Cloud Sync:

1. افتح التطبيق
2. اذهب إلى **تكوين المتقدم** → **مزامنة سحابية**
3. أدخل رمز 6 أرقام (مثال: `123456`)
4. اضغط **إعداد المزامنة السحابية**
5. قم بإنشاء أو تعديل قالب
6. تحقق من ظهور رسالة "✅ تم الحفظ في السحابة"

### اختبار عبر الأجهزة:

1. افتح جهاز آخر أو متصفح آخر
2. أدخل نفس الرمز (مثال: `123456`)
3. اضغط **تحميل من السحابة**
4. يجب أن تظهر جميع القوالب!

---

## 📊 معلومات KV Namespace

| نوع | اسم | ID |
|-----|-----|-----|
| Production | PRESETS | `d1ce93d311c34504939483fbff10c523` |
| Preview | PRESETS_preview | `b68cd2fe859f47e5bf4a505cb870ff1a` |

---

## ⚠️ مهم

- الرمز السحابي (6 أرقام) هو مفتاح الوصول لقوالبك
- احفظه في مكان آمن
- كل من يملك نفس الرمز يمكنه الوصول لنفس القوالب
- يمكنك استخدام أرقام مختلفة لمجموعات مختلفة من القوالب

---

## 🎉 بعد اكتمال الربط

نظام المزامنة السحابية سيعمل بشكل كامل:
- ✅ حفظ تلقائي عند إنشاء/تعديل/حذف القوالب
- ✅ مزامنة عبر جميع الأجهزة والمتصفحات
- ✅ نسخ احتياطي سحابي آمن
- ✅ واجهة سهلة بالعربية والإنجليزية
